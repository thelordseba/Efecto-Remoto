import React from 'react';
import './resetPassword.css';

    function ConfirmBtn({onClick}) {
        return(
        <div onClick={onClick} className="btn-confirm">
            <h4 className='wrd-conf'>Confirmar</h4>
            
            </div>
        )
        
        
    }



export default ConfirmBtn;
